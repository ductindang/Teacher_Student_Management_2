@using BLL.DTOs
@using BLL.DTOs.Enum
@using BLL.Request
@using BLL.Response
@using ClientWeb.Helper.Extensions
@using Newtonsoft.Json

@model IEnumerable<StudentAttendanceResponse>
@{
    ViewData["Title"] = "Lớp học";
    Layout = "~/Views/Shared/_LayoutProject.cshtml";
}


@section HeaderStyles {
    <link href="~/plugins/table/datatable/datatables.css" rel="stylesheet" />
    <link href="~/plugins/table/datatable/extensions/col-reorder/col-reorder.datatables.min.css" rel="stylesheet" />
    <link href="~/plugins/table/datatable/extensions/fixed-columns/fixed-columns.datatables.min.css" rel="stylesheet" />
    <link href="~/css/customs/plugins/light/table/datatable/dt-global_style.css" rel="stylesheet" />
    <link href="~/css/customs/plugins/light/table/datatable/custom_dt_custom.css" rel="stylesheet" />

    <link href="/css/customs/assets/light/elements/custom-pagination.css" rel="stylesheet" type="text/css" />
    <link rel="stylesheet" type="text/css" href="/css/customs/assets/light/apps/blog-post.css">


    <link href="~/plugins/filepond/filepond.min.css" rel="stylesheet" />
    <link href="~/plugins/filepond/FilePondPluginImagePreview.min.css" rel="stylesheet" />
    <link href="~/plugins/tagify/tagify.css" rel="stylesheet" />
    <link href="~/css/customs/plugins/light/editors/quill/quill.snow.css" rel="stylesheet" />
    <link href="~/css/customs/plugins/light/tagify/custom-tagify.css" rel="stylesheet" />
    <link href="~/css/customs/plugins/light/filepond/custom-filepond.css" rel="stylesheet" />

    <link href="~/css/customs/assets/light/apps/blog-create.css" rel="stylesheet" />
    <style>
        .featured-image{
            background-size: cover;
            height: 650px;
            border-radius:20px;
            overflow:hidden;
        }

        .class-info-wrapper {
            margin-top: 40px;
        }

        .class-section {
            margin-bottom: 35px;
        }

        .section-title {
            font-size: 22px;
            font-weight: 600;
            margin-bottom: 20px;
            border-left: 4px solid #4361ee;
            padding-left: 10px;
        }

        .class-info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit,minmax(220px,1fr));
            gap: 20px;
        }

        .info-item {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
        }

        .info-label {
            display: block;
            font-size: 13px;
            color: #888;
        }

        .info-value {
            font-weight: 600;
            font-size: 15px;
        }

        .class-description {
            line-height: 1.8;
            font-size: 15px;
        }

            .class-description p {
                margin-bottom: 10px;
            }

        .class-extra {
            padding-left: 18px;
        }

            .class-extra li {
                margin-bottom: 8px;
                line-height: 1.6;
            }

        .empty-description {
            color: #999;
            font-style: italic;
        }

        .status-present {
            background-color: #d4edda;
            color: #155724;
        }

        .status-absent {
            background-color: #f8d7da;
            color: #721c24;
        }

        .status-late {
            background-color: #fff3cd;
            color: #856404;
        }






        .class-layout {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 30px;
            margin-top: 30px;
        }

        /* LEFT */
        .class-main {
            display: flex;
            flex-direction: column;
            gap: 25px;
        }

        .section-card {
            background: #fff;
            border-radius: 16px;
            padding: 22px;
            border: 1px solid #eee;
        }

        .section-title {
            font-size: 18px;
            font-weight: 700;
            margin-bottom: 15px;
        }

        /* highlight list */
        .highlight-list {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px 20px;
        }

        .highlight-item {
            font-size: 14px;
            color: #333;
        }

            .highlight-item::before {
                content: "✔";
                color: #28a745;
                margin-right: 8px;
            }

        /* description */
        .class-description {
            font-size: 14px;
            line-height: 1.6;
            color: #444;
        }

        /* RIGHT */
        .class-sidebar {
            position: sticky;
            top: 20px;
            height: fit-content;
        }

        .price-card {
            background: #fff;
            border-radius: 16px;
            padding: 20px;
            border: 1px solid #eee;
        }

        .price {
            font-size: 26px;
            font-weight: 700;
            color: #e53935;
            margin-bottom: 15px;
        }

        .info-list {
            margin-top: 10px;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            font-size: 14px;
            padding: 8px 0;
            border-bottom: 1px dashed #eee;
        }

            .info-row span:first-child {
                color: #777;
            }



        .attendance-card {
            background: #fff;
            border-radius: 16px;
            padding: 20px;
            border: 1px solid #eee;
            margin-top: 25px;
        }

        .attendance-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .attendance-title {
            font-size: 18px;
            font-weight: 700;
        }

        /* stats */
        .attendance-stats {
            display: flex;
            gap: 10px;
        }

        .stat-box {
            padding: 8px 14px;
            border-radius: 20px;
            font-size: 13px;
            font-weight: 600;
        }

        .stat-present {
            background: #e6f7ee;
            color: #1aa053;
        }

        .stat-absent {
            background: #fde8e8;
            color: #e53935;
        }

        .stat-late {
            background: #fff4e5;
            color: #f59e0b;
        }

        /* table */
        .table-attendance {
            width: 100%;
        }

            .table-attendance th {
                font-size: 13px;
                color: #666;
                padding: 10px;
                background: #f8f9fa;
            }

            .table-attendance td {
                padding: 12px;
                border-top: 1px solid #f1f1f1;
            }

            .table-attendance tr:hover {
                background: #fafafa;
            }

        /* select */
        .attendance-status {
            border-radius: 8px;
            font-size: 13px;
        }

        /* button */
        .save-btn {
            background: #28a745;
            border: none;
            padding: 10px 18px;
            border-radius: 10px;
            color: #fff;
            font-weight: 600;
        }

            .save-btn:hover {
                background: #218838;
            }
    </style>
}

@{
    var userJson = Context.Session.GetString("UserLogin");
    var user = new User();

    if (!string.IsNullOrEmpty(userJson))
    {
        user = JsonConvert.DeserializeObject<User>(userJson);
    }


    string backgroundImage;
    var classDetails = ViewBag.ClassDetails;

    if (classDetails.ClassImage != null)
    {
        var base64 = Convert.ToBase64String(classDetails.ClassImage);
        backgroundImage = $"data:image/jpeg;base64,{base64}";
    }
    else
    {
        backgroundImage = Url.Content("~/images/lightbox-5.jpeg");
    }

    // var students = ViewBag.Students;
    var teacherReviews = ViewBag.TeacherReviews;

    // Đánh giá chung trong lớp học
    var reviews = ViewBag.TeacherReviews as List<TeacherReview>;

    double avgRating = 0;
    int totalReviews = 0;

    if (reviews != null && reviews.Any())
    {
        totalReviews = reviews.Count;
        avgRating = reviews.Average(r => r.Rating);
    }
}

<div class="layout-px-spacing">

    <div class="middle-content container-xxl p-0">

        <div class="row layout-top-spacing">

            <div class="col-xxl-12 col-xl-12 col-lg-12 col-md-12 col-sm-12 mb-4">


                <div class="single-post-content">

                    <div class="featured-image" style="background: url('@backgroundImage') no-repeat center; background-size: cover;">

                        <div class="featured-image-overlay"></div>

                        <div class="post-header">

                            <div class="post-title">
                                <h1 class="mb-0">@classDetails.Name</h1>
                            </div>

                            <div class="post-meta-info d-flex justify-content-between">

                                <div class="media">
                                    <img src="~/images/th4.png" alt="profile">
                                    <div class="media-body">
                                        <h5>@classDetails.TeacherName</h5>
                                        <div class="teacher-rating">
                                            <span style="font-weight:600; color:#ffcece">@avgRating.ToString("0.0")</span>

                                            <span style="color:#ffc107; font-size:18px;">
                                                @{
                                                    int fullStars = (int)Math.Floor(avgRating);
                                                    bool halfStar = avgRating - fullStars >= 0.5;

                                                    for (int i = 0; i < fullStars; i++)
                                                    {
                                                        <span>★</span>
                                                    }

                                                    if (halfStar)
                                                    {
                                                        <span>☆</span>
                                                    }

                                                    for (int i = fullStars + (halfStar ? 1 : 0); i < 5; i++)
                                                    {
                                                        <span style="color:#ddd;">★</span>
                                                    }
                                                }
                                            </span>

                                            <span style="color:#ffcece">(@totalReviews đánh giá)</span>
                                        </div>
                                        <p>Ngày dạy: @classDetails.StartDate.ToString("dd/MM/yyyy") - @classDetails.EndDate.ToString("dd/MM/yyyy")</p>
                                    </div>
                                </div>

                                <div class="align-self-center">
                                    <button class="btn btn-success btn-icon btn-share btn-rounded">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-share-2"><circle cx="18" cy="5" r="3"></circle><circle cx="6" cy="12" r="3"></circle><circle cx="18" cy="19" r="3"></circle><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"></line><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"></line></svg>
                                    </button>
                                </div>

                            </div>

                        </div>

                    </div>

                    <div class="class-layout">

                        <!-- LEFT -->
                        <div class="class-main">

                            <!-- GIỚI THIỆU -->
                            <div class="section-card">
                                <div class="section-title">📘 Giới thiệu khóa học</div>

                                <div class="class-description">
                                    @if (!string.IsNullOrEmpty(classDetails.Description))
                                    {
                                        @Html.Raw(classDetails.Description)
                                    }
                                    else
                                    {
                                        <i>Chưa có mô tả cho khóa học này.</i>
                                    }
                                </div>
                            </div>

                            <!-- BẠN SẼ HỌC ĐƯỢC -->
                            <div class="section-card">
                                <div class="section-title">🎯 Bạn sẽ học được gì?</div>

                                <div class="highlight-list">
                                    <div class="highlight-item">Nắm vững kiến thức cơ bản</div>
                                    <div class="highlight-item">Áp dụng vào thực tế</div>
                                    <div class="highlight-item">Học cùng giảng viên chuyên môn</div>
                                    <div class="highlight-item">Có lộ trình rõ ràng</div>
                                </div>
                            </div>

                            <!-- THÔNG TIN THÊM -->
                            <div class="section-card">
                                <div class="section-title">📌 Thông tin lớp học</div>

                                <div class="highlight-list">
                                    <div class="highlight-item">Giảng viên: @classDetails.TeacherName</div>
                                    <div class="highlight-item">Khóa học: @classDetails.CourseName</div>
                                    <div class="highlight-item">Số lượng: @classDetails.MaxStudents học viên</div>
                                    <div class="highlight-item">
                                        Thời gian:
                                        @classDetails.StartDate.ToString("dd/MM")
                                        - @classDetails.EndDate.ToString("dd/MM")
                                    </div>
                                </div>
                            </div>

                        </div>


                        <!-- RIGHT -->
                        <div class="class-sidebar">

                            <div class="price-card">

                                <div class="price">
                                    @classDetails.CoursePrice.ToString("N0") VNĐ
                                </div>

                                @* <button class="btn btn-success enroll-btn">
                                    🚀 Đăng ký ngay
                                </button> *@

                                <div class="info-list">

                                    <div class="info-row">
                                        <span>Khai giảng</span>
                                        <span>@classDetails.StartDate.ToString("dd/MM/yyyy")</span>
                                    </div>

                                    <div class="info-row">
                                        <span>Kết thúc</span>
                                        <span>@classDetails.EndDate.ToString("dd/MM/yyyy")</span>
                                    </div>

                                    <div class="info-row">
                                        <span>Học viên tối đa</span>
                                        <span>@classDetails.MaxStudents</span>
                                    </div>

                                    <div class="info-row">
                                        <span>Giảng viên</span>
                                        <span>@classDetails.TeacherName</span>
                                    </div>

                                </div>

                            </div>

                        </div>

                    </div>

                    @if(user != null)
                    {
                        if(user.RoleId == (int)ERoleName.Teacher)
                        {
                            <div class="attendance-card">

                                <!-- HEADER -->
                                <div class="attendance-header">

                                    <div class="attendance-title">
                                        📋 Điểm danh lớp học
                                    </div>

                                    <div class="attendance-stats">
                                        <div class="stat-box stat-present">✔ Có mặt</div>
                                        <div class="stat-box stat-absent">✖ Vắng</div>
                                        <div class="stat-box stat-late">⏰ Trễ</div>
                                    </div>

                                </div>

                                <!-- ACTION -->
                                <div style="margin-bottom:15px; text-align:right;">
                                    <button id="saveAttendanceBtn" class="save-btn">
                                        💾 Lưu điểm danh
                                    </button>
                                </div>

                                <!-- TABLE -->
                                <table id="student-table" class="table-attendance">

                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Mã HV</th>
                                            <th>Học viên</th>
                                            <th>Giới tính</th>
                                            <th>Trạng thái</th>
                                        </tr>
                                    </thead>

                                    <tbody>
                                        @{
                                            int sttStd = 1;
                                        }

                                        @foreach (var student in Model)
                                        {
                                            <tr>
                                                <td>@sttStd</td>
                                                <td>@student.StudentId</td>
                                                <td>@student.FullName</td>
                                                <td>@(((EGender)student.Gender).GetDisplayName())</td>

                                                <td>
                                                    <select class="attendance-status form-select"
                                                            data-student-id="@student.StudentId">

                                                        <option value="0" selected="@(student.Status == null)">
                                                            -- Chọn --
                                                        </option>

                                                        <option value="1"
                                                                selected="@(student.Status == EAttendanceStatus.Present)">
                                                            ✔ Có mặt
                                                        </option>

                                                        <option value="2"
                                                                selected="@(student.Status == EAttendanceStatus.Absent)">
                                                            ✖ Vắng
                                                        </option>

                                                        <option value="3"
                                                                selected="@(student.Status == EAttendanceStatus.Late)">
                                                            ⏰ Đi trễ
                                                        </option>

                                                    </select>
                                                </td>
                                            </tr>

                                            sttStd++;
                                        }

                                    </tbody>

                                </table>

                            </div>
                        }
                    }
                    

                </div>


            </div>

        </div>

    </div>

</div>

@section Scripts {
    <script src="~/plugins/table/datatable/datatables.js"></script>
    <script src="~/plugins/table/datatable/extensions/col-reorder/col-reorder.datatables.min.js"></script>
    <script src="~/plugins/table/datatable/extensions/fixed-columns/fixed-columns.datatables.min.js"></script>

    <script src="~/plugins/editors/quill/quill.js"></script>
    <script src="~/plugins/filepond/filepond.min.js"></script>
    <script src="~/plugins/filepond/FilePondPluginFileValidateType.min.js"></script>
    <script src="~/plugins/filepond/FilePondPluginImageExifOrientation.min.js"></script>
    <script src="~/plugins/filepond/FilePondPluginImagePreview.min.js"></script>
    <script src="~/plugins/filepond/FilePondPluginImageCrop.min.js"></script>
    <script src="~/plugins/filepond/FilePondPluginImageResize.min.js"></script>
    <script src="~/plugins/filepond/FilePondPluginImageTransform.min.js"></script>
    <script src="~/plugins/filepond/filepondPluginFileValidateSize.min.js"></script>


    <script src="~/plugins/tagify/tagify.min.js"></script>
    <script src="~/js/customs/assets/apps/blog-create.js"></script>


    <script src="/js/teacher/teacher.js"></script>

    <script>
        $('#student-table').DataTable(
            {
                "dom": "<'dt--top-section'<'row'<'col-12 col-sm-6 d-flex justify-content-sm-start justify-content-center'l><'col-12 col-sm-6 d-flex justify-content-sm-end justify-content-center mt-sm-0 mt-3'f>>>"
                    + "<'table-responsive'tr>"
                    + "<'dt--bottom-section d-sm-flex justify-content-sm-between text-center'<'dt--pages-count  mb-sm-0 mb-3'i><'dt--pagination'p>>",
                "oLanguage": {
                    "oPaginate": {
                        "sPrevious": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-left"><line x1="19" y1="12" x2="5" y2="12"></line><polyline points="12 19 5 12 12 5"></polyline></svg>',
                        "sNext": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-right"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>'
                    },
                    "sEmptyTable": "Chưa có dữ liệu",
                    "sInfo": "Đang hiển thị trang _PAGE_ của  _PAGES_",
                    "sSearch": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-search"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>',
                    "sSearchPlaceholder": "Tìm kiếm...",
                    "sZeroRecords": "Không có bản ghi nào trùng khớp",
                    "sLengthMenu": "Kết quả :  _MENU_",
                },
                "stripeClasses": [],
                "lengthMenu": [7, 10, 20, 50],
                "pageLength": 10,
                "ordering": false
            });
    </script>

    <script>
        $('#teacher-table').DataTable(
            {
                "dom": "<'dt--top-section'<'row'<'col-12 col-sm-6 d-flex justify-content-sm-start justify-content-center'l><'col-12 col-sm-6 d-flex justify-content-sm-end justify-content-center mt-sm-0 mt-3'f>>>"
                    + "<'table-responsive'tr>"
                    + "<'dt--bottom-section d-sm-flex justify-content-sm-between text-center'<'dt--pages-count  mb-sm-0 mb-3'i><'dt--pagination'p>>",
                "oLanguage": {
                    "oPaginate": {
                        "sPrevious": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-left"><line x1="19" y1="12" x2="5" y2="12"></line><polyline points="12 19 5 12 12 5"></polyline></svg>',
                        "sNext": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-right"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>'
                    },
                    "sEmptyTable": "Chưa có dữ liệu",
                    "sInfo": "Đang hiển thị trang _PAGE_ của  _PAGES_",
                    "sSearch": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-search"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>',
                    "sSearchPlaceholder": "Tìm kiếm...",
                    "sZeroRecords": "Không có bản ghi nào trùng khớp",
                    "sLengthMenu": "Kết quả :  _MENU_",
                },
                "stripeClasses": [],
                "lengthMenu": [7, 10, 20, 50],
                "pageLength": 10,
                "ordering": false
            });
    </script>

    <script>
        const btn = document.getElementById("saveAttendanceBtn");

        if (btn) {
            btn.addEventListener("click", async function () {

                const scheduleId = @ViewBag.ScheduleId;
                const classId = @classDetails.Id;

                const students = [];
                let hasEmpty = false;

                document.querySelectorAll(".attendance-status").forEach(el => {

                    const studentId = parseInt(el.dataset.studentId);
                    const status = parseInt(el.value);

                    if (status === 0) {
                        hasEmpty = true;
                    }

                    students.push({
                        studentId: studentId,
                        status: status
                    });

                });

                // Nếu có học viên chưa chọn
                if (hasEmpty) {
                    alert("Vui lòng chọn trạng thái điểm danh cho tất cả học viên.");
                    return;
                }

                const request = {
                    scheduleId: scheduleId,
                    classId: classId,
                    students: students
                };

                const response = await fetch("/Attendance/TakeAttendance", {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json"
                    },
                    body: JSON.stringify(request)
                });

                if (response.ok) {
                    alert("Điểm danh thành công");
                } else {
                    alert("Có lỗi xảy ra");
                }

            });
        }
    </script>
}

